services:
  licensing-server:
    build: .
    container_name: cineflow-licensing
    ports:
      - "3002:3002"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
